## 2025-02-23 - [Optimize NumPy Top-K Selection with argpartition]
**Learning:** `np.argsort` has O(N log N) complexity, causing performance bottlenecks during top-K candidate selection in large vector search indices (e.g., K3 MRL Indexer). For top-K selection without a full sort, `np.argpartition` provides O(N) complexity. In benchmarks, switching from `argsort` to `argpartition` for K=75 out of 100,000 vectors reduced the execution time from ~4.0ms down to ~0.36ms (a 10x+ improvement).
**Action:** When extracting top-K candidates from large NumPy arrays (e.g., scoring matrices, similarity calculations), always prioritize `np.argpartition` followed by sorting just the selected partition, rather than using `np.argsort` on the entire array.

## 2025-02-23 - [Optimize NumPy Matrix/Vector Normalization]
**Learning:** `np.linalg.norm` for matrices and vectors can be significantly slower than directly calculating the squared norm and taking the square root. For matrix row norms, `np.sqrt(np.einsum('ij,ij->i', A, A))` avoids intermediate array overhead and is much faster than `np.linalg.norm(..., axis=1)`. For 1D vectors, `np.sqrt(np.dot(v, v))` is faster than `np.linalg.norm(v)`. In our benchmark, this approach yielded a ~4.8x speedup.
**Action:** When calculating L2 norms for cosine similarity or normalization, use `np.einsum` for matrices and `np.dot` for 1D arrays instead of `np.linalg.norm` to avoid unnecessary overhead in hot paths.
